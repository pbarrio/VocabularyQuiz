<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bikebot.vocabularyquiz">

    <!-- TODO: make a proper launcher -->
    <!-- TODO: think of a proper name -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddWordActivity" />
        <activity android:name=".QuizActivity" />
        <activity
            android:name=".ConfigOptionsActivity"
            android:label="@string/title_activity_config_options" />
        <activity android:name=".ErrorMsgActivity" />
        <activity android:name=".ResultsActivity" />
        <activity
            android:name=".ListDictionaryActivity"
            android:label="@string/title_activity_list_dictionary" />
        <activity
            android:name=".ListConfigOptionsActivity"
            android:label="@string/title_activity_list_config_options"></activity>
    </application>

</manifest>